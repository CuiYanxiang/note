## 链表

### 关键字

>
>
> 哨兵节点、哈希表

### 哨兵节点

```text
简化链表边界条件而引入附加链表节点，通常位于链表的头部
```

### 双指针

```text
前后指针
    1.即一个指针在链表中提前朝着指向下一个节点的指针移动若干步，然后移动第2个指针
    2.通常查找链表的倒数第K个节点。先让第1个指针从链表的头节点开始朝着指向下一个节点的指针先移动k-1步，然后让第2个指针指向链表的头节点，再让两个指针以相同的速度一起移动，当第1个指针到达链表的尾节点时第2个指针正好指向倒数第k个节点
快慢指针
    1.即两个指针在链表中移动的速度不一样，通常是快的指针朝着指向下一个节点的指针一次移动两步，慢的指针一次只移动一步。采用这种方法，在一个没有环的链表中，当快的指针到达链表尾节点的时候慢的指针正好指向链表的中间节点。
```

```java
public ListNode reverserList(ListNode head){
        ListNode prev = null;
        ListNode cur = head;
        while(cur != null){
            ListNode next = cur.next;
            cur.next = prev;
            prev = cur;
            cur = next;
        }
        return prev;
}
```